<div class="container mt-12 mb-16" style="max-width: 800px;" data-section-id="{{ section.id }}">
  <h1 style="font-family: var(--font-heading); margin-bottom: 2rem;">{{ page.title }}</h1>
  <div style="color: var(--color-text-secondary); line-height: 1.8;">
    {{ page.content }}
  </div>
</div>

{% schema %}
{
  "name": "Pagina Standard",
  "settings": []
}
{% endschema %}
